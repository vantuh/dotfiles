# OS / editor
.DS_Store
Thumbs.db
*.swp
*~

# Dependencies / build
node_modules/

# Local databases
*.db
*.db-shm
*.db-wal

# Secrets and local overrides (never commit)
.env
.env.*
!.env.example
.tokens
**/.tokens
zsh/.zshrc.d/99-local.zsh
zsh/.zcompdump*
secrets/
credentials*
*.pem
*.key
*.p12
id_rsa*
*.secret.json
*.private.json
*.local.json

# Logs and agent runtime
*.log
**/sessions/
**/.pi/sessions/
agents/.pi/

# herdr runtime state (only config.toml is versioned)
herdr/.config/herdr/session.json
herdr/.config/herdr/*.sock

# Generated Kiro ACP bridge agent config
.kiro/agents/pi-kiro-*.json

# Generated by skill tools (circular symlink only)
agents/.agents/skills/skills

# Editor backups
*.bak
# Repo-local skills/prompts only; the pi/ stow package's .pi stays versioned
/.pi/
