# План: інтерактивні агенти

Портування ідей з [pi-interactive-subagents](https://github.com/amosblomqvist/pi-interactive-subagents)
(форк HazAT) у `herdr-agents`. Джерело — асинхронний tmux-плагін; ми лишаємось на Herdr
і на синхронній моделі, тому портуємо **фічі**, а не архітектуру.

Чотири фази, **кожна виконується окремо і мержиться окремо**. Наступна фаза не
починається, поки попередня не пройшла свій gate.

## Порядок і гейти

| Фаза | Що | Ризик | Залежності |
|---|---|:---:|---|
| 0 | Ліміт паралельних агентів 2–3 → 4–5 | низький | — |
| 1 | Widget статусів агентів | низький | — |
| 2 | Двонапрямлений канал (`ask_question`) | середній | фаза 1 (статус `waiting` у widget) |
| 4 | Доставка результату асинхронного агента | середній | фаза 1 (поллер як фоновий спостерігач) |
| 3 | Loadout snapshot | низький | — |

**Порядок переглянуто після фаз 1–2.** Спочатку фаза 3 стояла перед 4 як її
передумова, а фаза 4 вважалась високоризиковою зміною архітектури. Обидві
оцінки виявились хибними:

- фаза 4 **не** спирається на loadout snapshot — доставка тримається на
  поллері з фази 1 і прапорці в state-файлі;
- фаза 4 **не** замінює блокуючу модель, а додає другий режим, тому нічого з
  наявного (soft re-wait, дефолт `wait: true`, раннє повернення питання) не
  скасовується.

Тож фаза 3 лишається останньою як найменш цінна: вона дає попередження про
розбіжність профілю при reuse, тоді як фаза 4 закриває реальну діру — результат
асинхронного агента зараз може просто пропасти.

## Перевірені API

Не вигадувати нові механізми — все нижче вже існує:

- `ctx.ui.setWidget(id, lines | component, { placement: "aboveEditor" })` —
  `docs/extensions.md`, приклади компонентів у `docs/tui.md`. Знімається
  передачею `undefined`.
- `pi.sendMessage({ customType, content, display, details }, { triggerTurn: true, deliverAs: "steer" })` —
  `docs/extensions.md` (`pi.sendMessage`). Потрібно лише у фазі 4.
- `pi.registerMessageRenderer(customType, …)` — кастомний рендер для власних
  `customType` повідомлень.
- `ctx.isIdle()` — гард «стрімить чи ні». `deliverAs` обов'язковий лише під час
  стрімінгу; `sendUserMessage` без нього кидає помилку. Патерн у
  `examples/extensions/send-user-message.ts`.
- Herdr `api snapshot` → стан кожного агента (`agent_status`,
  `state_change_seq`, `interactive_ready`). Уже парситься: `parseAgentSnapshot`
  (`herdr.ts`), доступ через `getAgentSnapshot` / `getSessionSnapshot`.
- Канал батько→дитина вже є: `promptAgent` (`herdr.ts`) → `herdr agent prompt`.

## Що свідомо не портуємо

- **tmux surface layer** — у нас Herdr із детермінованим 60/40 layout-ом і
  placement-локом. Форк робить `even-horizontal` після кожного spawn/exit.
- **`session-mode: fork`** — свідоме рішення: свіжий контекст замість
  форкнутої розмови (`docs/README.md`, "Known design choices").
- **Рекурсивний спавн (`subagent_agents` + `PI_SUBAGENT_ALLOWED`)** — у нас
  `HERDR_AGENT_CHILD=1` жорстко вирубає делегацію в дитині. Не змінюємо.
- **`registerToolExtension`** — рантайм-реєстрація інструментів третіми
  extension-ами. Немає споживача.

---

## Фаза 0 — Ліміт паралельних агентів (2–3 → 4–5)

### Поточний стан

**Ліміту в коді немає.** Ніде в extension-і не перевіряється кількість
одночасних агентів — немає ні `MAX_*` константи, ні гарди в тулзі. "2–3 max" —
це виключно текст у промптах, тобто модель може його проігнорувати, а може
й перевиконати (обмежитись двома там, де чотири доречні).

Цільове формулювання — **4–5 max**, а не жорстка п'ятірка: діапазон лишає
моделі свободу зупинитись на чотирьох, коли п'ятий агент не додає незалежної
роботи, і одночасно робить 5 легальним, а не порушенням політики.

Три місця з формулюванням:

1. `constants.ts` → `GLOBAL_INSTRUCTIONS`: *"Launch parallel `herdr_agent`
   calls only for independent reads or disjoint write slices (2–3 max)"* —
   це реально інжектиться в системний промпт Orchestrator-а.
2. `docs/README.md` → **Negative policy**: *"(2–3 agents max)"*.
3. `AGENTS.md` (плагіна) — таблиця політики делегування.

Плюс те саме є в спільних інструкціях репозиторію:
`agents/.agents/AGENTS.md` → *"keep to 2–3 agents"*. Це **не файл плагіна**, і
правило репо (`dotfiles/AGENTS.md`) прямо забороняє його правити без потреби,
бо він спільний для всіх агентів. Змінювати лише з явного дозволу
користувача — інакше плагін і глобальна політика розійдуться в цифрах.

### Що технічно вже витримує 5

- **Placement lock** (`acquirePanePlacementLock`) серіалізує спавни, тому
  5 паралельних викликів створять один правий стовпець, а не 5 конкурентних
  колонок. Кількість не має значення для коректності.
- **Rebalance** (`buildEqualAgentSplitRatios`) рахує ratio з фактичної
  кількості керованих пейнів — формула вже загальна, `1/5` виходить сама.
  Ніякого хардкоду на трьох нема.
- **State-файл** (`state.ts`) — черга на запис per-file, кількість не обмежена.

### Що тисне на верхню межу

- **Читабельність пейнів.** 5 агентів у 40% колонці = ~1/5 висоти терміналу
  кожен. На типовому 50-рядковому вікні це ~10 рядків на агента з рамкою —
  на межі корисності. Це головний реальний обмежувач, а не код, і саме тому
  верхня межа 5, а не 6+.
- **Паралельні `agent wait`.** Кожен виклик тримає власний `execHerdr`
  процес до завершення. Треба переконатись, що ліміт одночасних tool-call-ів
  Pi пропускає 5.
- **Розмір результату.** 5 артефактів по 180 рядків fallback-у в один turn
  Orchestrator-а — реальний ризик роздути контекст. Аргумент за фазу 4
  (асинхронна доставка розносить результати по turn-ах).

### Рішення

- **Жорсткого гарду в коді не робимо.** Ніякого `HERDR_AGENTS_MAX_PARALLEL`
  чи перевірки кількості в тулзі — межа лишається промпт-політикою. Причина:
  гард заблокував би легітимний випадок 6 незалежних читань, а вартість
  порушення низька (просто тісні пейни).
- **`agents/.agents/AGENTS.md` оновлено** з явного дозволу користувача, щоб
  плагін і глобальна політика не розходились у цифрах.

### Виконано

- [x] `constants.ts` → `GLOBAL_INSTRUCTIONS`: `(2–3 max)` → `(4–5 max)` плюс
      уточнення *"the limit is independence, not count — go past 2–3 only when
      every agent has genuinely independent work"*.
- [x] `docs/README.md` → **Negative policy**: `(4–5 agents max)` + причина
      верхньої межі (висота пейна в 40% колонці).
- [x] `agents/.agents/AGENTS.md` → *"keep to 4–5 agents"*.
- [x] `AGENTS.md` плагіна — цифри не містить, синхронізація не потрібна.
- [x] Smoke-тест на 5 агентів пройдено (Herdr 0.8.2, термінал 65×272).

### Результат smoke-тесту

**Стадія 1 — layout на 5 живих агентах.** `herdr api snapshot` показав рівно
одну структуру, без конкурентних колонок:

```
split_0_root   right  ratio 0.6        → Orchestrator 163 cols | колонка 109 cols
split_1_1      down   ratio 0.8        = 4/5
split_2_10     down   ratio 0.75       = 3/4
split_3_100    down   ratio 0.6666667  = 2/3
split_4_1000   down   ratio 0.5        = 1/2
```

Вкладені ratio `4/5 · 3/4 · 2/3 · 1/2` дають точні п'ятини: усі 5 пейнів
`height 13` при `y = 1, 14, 27, 40, 53`, сума 65 = повна висота табу. Усі 6
пейнів (Orchestrator + 5 агентів) в одному табі, колонка одна (`x = 192`).

**13 рядків на агента** — це і є практична межа з розділу вище. Оцінка в плані
була ~10 рядків на 50-рядковому вікні; на 65-рядковому вийшло 13. Шостий агент
дав би ~11, сьомий ~9.

**Стадія 2 — повний цикл 5 паралельних one-shot.** Усі 5 повернули
`HERDR_RESULT: status: done`. Після завершення: у табі лишився тільки
`w2V:p1X` (Orchestrator), `splits: 0`, жодного залишкового пейна,
state-файл чистий — `{"version":1,"agents":{}}`.

**Побічна знахідка (не про інфраструктуру).** Три з п'яти агентів дали
неправильні числа: `utils.ts` 181 замість 190, `agents.ts` 79 замість 87,
`schema.ts` 41 замість 37. Профіль `scout` працює на
`kiro-acp/claude-haiku-4.5`, і на точному підрахунку рядків він ненадійний.
Делегування спрацювало штатно — помилився зміст, не транспорт. Це емпіричне
підтвердження наявного правила «Orchestrator must synthesize child results,
not blindly forward them»: перевіряй числа з дитячого звіту, якщо вони
важливі для рішення.

### Gate — пройдено

П'ять паралельних one-shot агентів проходять повний цикл: спавн → рівний
layout → 5 результатів → закриття, без осиротілих пейнів і без конкурентних
колонок. Перевірено на верхній межі діапазону (5, не 4). Фаза 0 закрита.

---

## Фаза 1 — Widget статусів агентів

### Мета

Панель над інпутом Orchestrator-а зі станом усіх керованих агентів, щоб не
тримати в голові, хто зараз працює і як довго.

```
╭─ Herdr agents ───────────────────────── 2 running ─╮
│ 00:23  Scout — message-bus   working                │
│ 00:45  Reviewer              blocked · needs attach │
╰─────────────────────────────────────────────────────╯
```

### Дизайн

Ключова відмінність від форку: **дитина нічого не пише**. Форк тримає
activity-снапшот, який дитина оновлює сама, бо tmux не знає про стан агента.
Herdr знає — `api snapshot` уже віддає `agent_status` per-agent. Тому widget —
це чисто читаючий поллер над уже наявними даними.

Джерела:

- `listCurrentWorkspaceAgents` / `listManagedWorkspaceAgents` (`herdr.ts`) —
  які pane/tab керовані;
- `loadHerdrAgentsState` (`state.ts`) — label, профіль, lifecycle;
- `api snapshot` — `agent_status` + `interactive_ready`.

Стани для показу: `starting` (агент стартував, ще нема снапшоту), `working`,
`blocked` (Herdr віддає blocked / `agent_blocked` — підказка "attach"),
`waiting` (з'явиться у фазі 2), `idle`/`done`.

### Кого widget показує

Не всіх керованих агентів. Widget існує, щоб показувати тих, за кем ніхто
не спостерігає, а при `wait: true` оркестратор і так стоїть у тулзі, яка
рендерить `Waiting for Herdr agent <label>...` (плюс `herdr:blocked` підсвічує
пейн оркестратора в Herdr). Рядок у widget-і був би третьою копією того самого
факту, і при 4–5 паралельних синхронних викликах це 5 рядків над інпутом без
нової інформації.

Правило: **приховувати агентів, на яких оркестратор зараз блокується, крім
тих, чий статус `blocked`.**

Виняток. `blocked` **мав би** покривати випадок, коли дитина сидить на
approval-діалозі, бо tool-call цього не розрізняє — він показує те саме
`Waiting for…` до самого таймауту.

**Уточнення після гейта статусів у фазі 2: цей виняток на практиці не
спрацьовує.** Експеримент показав, що сесія на діалозі віддає `working`, а
`blocked` виставляється лише з події `herdr:blocked`, тобто коли *оркестратор*
чекає на дитину — у самій дитині вона не емітиться ніколи. Отже для керованих
дітей `blocked` недосяжний, і фільтр фактично приховує **всіх** очікуваних
агентів, тобто поводиться як початковий варіант «тільки асинхронні».

Умову `status === "blocked"` лишено як захисну: вона нічого не коштує, коректна
за змістом, і почне працювати сама, якщо Herdr колись почне детектувати діалоги
або якщо ми додамо власний сигнал. Але покладатись на неї як на видимість
застряглої дитини не можна — цю прогалину фаза 2 закриває файлом
`question.md`, а не статусом.

Реалізація — in-process `Set` labels (`awaitedAgentLabels` в `index.ts`), а не
поле в стейті: це властивість стека викликів цієї сесії, не агента, і так воно
автоматично коректне і на шляху re-wait. Блокуюче очікування має рівно дві
точки входу і два `finally`, тому пари симетричні.

`herdr:blocked` для цього **не** використовується: його payload — контракт,
який споживає власне розширення Herdr (`extensions/herdr-agent-state.ts`), тому
подія лишається незмінною.

Наслідок для поллера: порожній список видимих ≠ порожній список керованих.
Поллер зупиняється тільки коли керованих агентів нема взагалі. Якщо агенти є,
але всі приховані, тік продовжується — інакше перехід у `blocked` ніколи б не
з'явився, бо ніщо не перезапустило б поллер.

### Файли

- новий `widget.ts` — рендер рядків + мапінг `agent_status` → підпис;
- новий `runtime.ts` (або секція у `state.ts`) — зведення
  managed state + snapshot в один масив `AgentRuntimeInfo`;
- `index.ts` — таймер (1000 ms), `ctx.ui.setWidget`, знімання widget-а коли
  керованих агентів нема, cleanup інтервалу;
- новий `widget.test.ts` — рендер за фіксованим входом.

### Задачі

- [x] `types.ts`: `HerdrAgentInfo.updatedAt?: string` (passthrough зі стейту).
- [x] `herdr.ts`: `listManagedWorkspaceAgents` віддає `updatedAt`.
- [x] `widget.ts`: чисті `formatElapsed`, `agentStatusView`, `truncateLabel`,
      `renderAgentWidgetLines` — без I/O.
- [x] `index.ts`: `loadAgentsForWidget` (без prune/save), `updateAgentsWidget`,
      `ensureAgentsWidget`, стоп поллера на порожньому списку, гард `ctx.hasUI`,
      очищення застарілого таймера через `globalThis` при `/reload`.
- [x] Поллер стартує з `session_start` і після створення пейна в тулзі.
      `session_start` фаєриться і з `reason: "reload"`, тому `/reload` підіймає
      widget без нової сесії.
- [x] `session_shutdown` зупиняє поллер — покриває quit/new/resume/fork, а не
      лише reload-шлях через `globalThis`. Документований патерн для
      session-scoped ресурсів.
- [x] Child-режим не реєструє поллер: `HERDR_AGENT_CHILD=1` виходить із
      `herdrAgentsExtension` раніше.
- [x] `widget.test.ts` — 13 тестів: усі статуси, 0/1/2 агенти, обрізка,
      вирівнювання колонок, відсутній/невалідний `updatedAt`, paint.
- [x] `widget.ts`: чиста `visibleWidgetAgents(agents, awaitedLabels)` —
      приховує очікуваних, крім `blocked`.
- [x] `index.ts`: `awaitedAgentLabels` + `beginAwaitingAgent` /
      `endAwaitingAgent` у двох парах вхід/`finally` блокуючого очікування
      (шлях спавну-reuse і шлях re-wait).
- [x] `index.ts`: `updateAgentsWidget` розділяє «нема керованих» (стоп поллера)
      і «нема видимих» (лише очистити widget, тік продовжується).
- [x] `widget.test.ts` — 5 тестів фільтра: нікого не чекаємо, очікуваний
      прихований, очікуваний + `blocked` показаний, усі очікувані, точний матч
      label без префіксного співпадіння.

### Відхилення від початкового задуму

- **`runtime.ts` не створювався.** `listManagedWorkspaceAgents` +
  `loadHerdrAgentsState` уже дають готовий `HerdrAgentInfo[]`, тому окремий
  модуль зведення був би зайвим шаром.
- **Нове поле `startedAt` не додавалось.** У стейті вже є `updatedAt`, і тулза
  перезаписує його на кожному спавні *і* на кожному persistent-reuse — це
  точно та точка відліку, що потрібна («час у поточній задачі», не «вік
  пейна»).
- **Без рамки.** Замість box-компонента з `render(width)` — простий
  `string[]`, який `setWidget` приймає напряму. Ширина не потрібна: labels
  обрізаються до 28 символів, колонки вирівнюються між рядками.

### Верифікація

`bun test` — 84 pass / 0 fail (з них 18 нових), `bun build` збирається
(61 KB), `prettier --check *.ts` чисто.

Живий дата-шлях перевірено на **двох реальних керованих агентах** (спавн через
`herdr_agent`, `lifecycle: persistent`), семплінг `getCurrentContext` →
`listManagedWorkspaceAgents` → `renderAgentWidgetLines` раз на 3 с:

```
Herdr agents · 2 · 1 working
 00:03 Widget alpha                  working
 00:36 Widget truncation check lon…  done
```

Через кілька секунд, після завершення роботи:

```
Herdr agents · 2
 00:06 Widget alpha                  done
 00:39 Widget truncation check lon…  done
```

Підтверджено на живих даних:

- Реальні `agent_status` мапляться: `working` → активний тон + суфікс
  `· 1 working` у заголовку; `done` → тихий тон, суфікс зникає.
- Час тікає між семплами (00:13 → 00:16 → 00:19).
- **`updatedAt` дійсно перезаписується на persistent-reuse:** після повторного
  промпта в `Widget alpha` відлік скинувся з 00:36 на 00:03. Це емпірично
  підтверджує вибір `updatedAt` замість нового `startedAt` — показуємо «час у
  поточній задачі», а не вік пейна.
- Truncation і вирівнювання колонок на реальних labels.
- Після `pane close` обох агентів: `0 agents` → `renderAgentWidgetLines`
  віддає `[]` → `setWidget(id, undefined)` і поллер зупиняється. Таб
  повернувся до `panes: 1, splits: 0`.

Побічно підтверджено рішення «widget не робить prune»: закриття пейнів в обхід
плагіна лишило два мертвих записи в state-файлі, але widget показав `0 agents`,
бо `listManagedWorkspaceAgents` фільтрує по живих пейнах. Наступний
`pruneHerdrAgentsState` проти живого снапшоту прибрав обидва
(`before: 2 → after: 0`). Тобто стейл-записи не течуть у відображення і
підчищаються штатними шляхами.

Не покрито живим прогоном: `blocked` (вимагає агента на approval-діалозі) —
покритий юніт-тестом; `status` тут прямий проброс `pane.agent_status`.

### Ручна перевірка

`/reload`, спавн двох агентів, widget показує обох з тривалістю і статусом,
після закриття всіх — зникає.

Дата-шлях цього сценарію відпрацьовано (див. Верифікацію), візуальне
підтвердження в TUI отримано (див. Gate).

### Gate — пройдено

- [x] Widget оновлюється під час реального `herdr_agent` виклику — статус
      перейшов `working` → `done`, час тікає між тіками.
- [x] Зникає після закриття всіх агентів — `0 agents` → `setWidget(undefined)`,
      поллер стоп.
- [x] `bun test` зелений (84 pass / 0 fail), `prettier --check` чисто,
      extension завантажується (`pi -p` без помилок).
- [x] Жодного зайвого `herdr` виклику частіше ніж раз на тік — `widgetTicking`
      відсікає накладання тіків, `loadAgentsForWidget` робить один
      `api snapshot` на тік і не пише стейт.
- [x] Візуальна перевірка в TUI — підтверджено користувачем:
      синхронний агент у панелі не з'являється (працює в правій колонці, але
      рядка нема, бо його стан друкує tool-call); при блокуючому виклику на
      єдиного керованого агента панель зникає повністю і **повертається** після
      завершення з `idle` — тобто поллер переживає порожній список видимих.

### Ризики

- Поллер плюс блокуючий `agent wait` в одному процесі — переконатись, що
  `execHerdr` з таймера не змагається з очікуванням за той самий AbortSignal.
- Тік раз на секунду × `api snapshot` — якщо дорого, підняти до 2000 ms.

---

## Фаза 2 — Двонапрямлений канал (`ask_question`)

### Мета

Дитина може задати батькові одне питання і **не вмерти**: сесія паркується,
батько відповідає, дитина продовжує з відповіддю. Це закриває наш найгірший
failure mode — зараз агент, що впирається в питання, дає `agent_blocked`
(див. `51bd683`), і єдиний вихід — attach руками.

### Ключова відмінність від форку

Форк робить steer у сесію батька, бо він асинхронний. **У нас це не працює**:
батько сидить всередині свого tool-call (`herdr_agent` з `wait: true`) і не
може взяти новий turn, поки тулза не повернеться. Тому:

> питання дитини завершує поточний `herdr_agent` виклик **раннім успішним
> результатом** зі статусом `question`, а не steer-ом.

Батько відповідає наступним викликом `herdr_agent` з тим самим `tabLabel` і
`task` = відповідь → `promptAgent` шле її в ту саму сесію. Дитина лишається
жива весь час. Це повне перевикористання наявного persistent-reuse шляху,
без нової транспортної магістралі до батька.

Наслідок: `lifecycle: "oneshot"` при питанні **не закриває** target — агент
лишається відкритим до фінального результату. Це вже узгоджується з правилом
"one-shot закривається лише після успішного завершення".

**Знайдено живим тестом:** цього не досить. Reuse пейна за label був
обмежений `if (persistent)`, тому відповідь на запаркований one-shot
створювала **нового** агента і осиротювала запаркованого назавжди. Умову
розширено: пейн переиспользується також коли на диску є невідповідане питання
(`persistent || answeringQuestion`). Інакше канал працював би тільки для
persistent, тобто в меншості реальних делегувань.

### Гейт allowlist — пройдено

Блокуюча передумова: чи extension-тулза взагалі доживає до дитини, яку ми
запускаємо з `--tools`. Перевірено емпірично одноразовим пробним розширенням
(`pi -e`, друкує `getActiveTools()` / `getAllTools()` на `session_start`):

| Випадок | `--tools` | Результат |
| --- | --- | --- |
| A | `read,probe_tool` | `active = ["read","probe_tool"]` — extension-тулза активна |
| B | `read` | `active = ["read"]`, **`all = ["read"]`** — тулзи нема в реєстрі |
| C | `read` + `setActiveTools([...active,"probe_tool"])` | без змін: `["read"]` |
| D | `read,herdr_agent` | `["read","herdr_agent"]` — авто-виявлені розширення так само |

Висновки, що визначають дизайн:

1. **Extension-тулзу можна дати дитині — але тільки назвавши її в `--tools`.**
   Це під нашим контролем: `piArgs` будується в `index.ts` (~930).
2. **Дитина не може доввімкнути себе сама.** `--tools` фільтрує *до* активного
   набору — тулза зникає з `getAllTools()`, а `setActiveTools` ігнорує
   незареєстровані імена (випадок C). Тобто варіант «child-режим сам додає
   `ask_question`» відпадає, фільтрація застосовується після реєстрації
   розширень.
3. `--tools` передається лише коли профіль має непорожній `tools`
   (`if (agent.tools?.length)`). Для профілів **без** списку тулз прапорця
   немає, тож `ask_question` буде доступна автоматично. Додавати ім'я треба
   тільки в гілці, де список є.

### Гейт статусів — що Herdr реально віддає

Перед вибором механіки очікування перевірено експериментом: одноразове
розширення з тулзою, яка викликає `ctx.ui.confirm`, запущене як Pi-агент у
власному пейні (`herdr agent start qprobe --kind pi ... -e dialog.ts`).

Результат — **сесія, що стоїть на діалозі-питанні, віддає
`agent_status: working`**, стабільно (8 семплів × 3 с, `state_change_seq`
не рухається). Скрін пейна одночасно показував відкритий діалог із
`→ Yes / No`. Ніякого `blocked`.

Причина в тому, що статус публікується, а не детектується:
`pi/.pi/agent/extensions/herdr-agent-state.ts` віддає лише
`working | blocked | idle`, і `blocked` там виставляється **виключно** з події
`herdr:blocked`, тобто коли *оркестратор* чекає на дитину. У снапшоті поряд
стоїть `screen_detection_skipped: true` — Herdr свідомо не гадає по екрану,
коли агент репортує сам.

Наслідки:

1. **Дитина ніколи не віддасть `blocked`.** Делегувати вона не може, отже
   `herdr:blocked` у ній не емітиться. Для керованих дітей статус ∈
   `working | idle | done`.
2. **Варіант B (`--until blocked`) мертвий.** Ловити нічого.
3. **Запаркована дитина не стає `idle`** — turn ще активний, поки тулза висить
   на діалозі. Тому `ask_question` **не має блокувати** turn.

Окремо виявлено пастку: `herdr agent prompt` у запаркованого агента
завершується успішно (`agent_prompted`), але `state_change_seq` не зростає, а
натиснуті клавіші **відповідають за діалог** — після другого промпта діалог
зник сам. Тобто промпт у запаркованого агента тихо ламає його стан. Наша
перевірка приймання промпту через зростання `state_change_seq` (`083770e`)
захищає саме від цього і виявляється важливішою, ніж описано в її коміті.

### Транспорт дитина→батько

Файл `question.md` у тому ж temp-наборі, що й result-файл
(`createAgentTempFiles` в `utils.ts`).

Два відхилення від початкового задуму, обидва на користь простоти:

- **`question.md`, не `question.json`.** Питання — це один вільний текст;
  JSON не додав би нічого, крім парсингу. Валідація шляху та сама, що для
  `result.md` (`isManagedAgentFile`).
- **Без другого маркера в промпті.** Шлях питання виводиться з
  `HERDR_RESULT_FILE:`, який дитина вже отримує: `questionFileFor(resultFile)`
  бере ту саму теку. Один маркер — одне джерело істини.

Механіка — **варіант C**, простіший за обидва початкові:

1. `ask_question` пише `question.md` і **одразу повертається** (жодного
   діалогу, жодного блокування).
2. `CHILD_PROTOCOL` вимагає завершити turn відразу після питання, без
   подальших tool-call-ів і без `HERDR_RESULT`.
3. Turn закінчується → дитина стає `idle` → **наявний**
   `herdr agent wait --until idle --until done` спрацьовує сам.
4. Батько перед читанням результату перевіряє `question.md`: якщо файл є —
   рання віддача зі статусом `question`, one-shot не закривається.

Тобто ні паралельного поллінгу, ні скасування wait, ні нових прапорців:
використовується той самий шлях пробудження, яким сьогодні приходить кожен
нормальний результат. Варіанти A і B відкинуті.

Ціна: дитина, що впала в **справжній** approval-діалог (не наш
`ask_question`), лишається `working` і невідрізнима від робочої аж до
таймауту. Це не регресія — так є і зараз — але й фаза 2 цього не лікує.

### Файли

- `utils.ts` — `createQuestionFile`, `readAgentQuestion`, `clearAgentQuestion`;
- `constants.ts` — `CHILD_PROTOCOL`: коли можна питати (неоднозначні вимоги,
  рішення, що змінює обсяг), що робити далі, заборона питати про дрібниці;
- новий `child.ts` — виніс child-режиму з `index.ts`: result writer +
  реєстрація тулзи `ask_question`;
- `index.ts` — дописати `ask_question` до `--tools` дитини **лише** коли
  профіль задає `tools` (інакше прапорця немає і тулза доступна й так);
  гілка результату `status: "question"`;
- `herdr.ts` — гачок скасування очікування при питанні;
- `schema.ts` — уточнити опис `task`: він же канал відповіді на питання;
- `widget.ts` — позначка «є питання без відповіді». Взяти її з `agent_status`
  **не вийде**: дитина після питання завершує turn і стає `idle`, тобто
  невідрізнима від persistent-агента, який просто дочекався. Потрібен окремий
  прапорець у state-записі (напр. `pendingQuestion`), який ставиться при ранній
  віддачі зі статусом `question` і знімається при наступному промпті.

### Задачі

- [x] Виділити child-режим у `child.ts`, зберігши поведінку result writer-а.
- [x] Зареєструвати `ask_question` тільки при `HERDR_AGENT_CHILD=1`; параметр `question: string`, повертає підтвердження "питання надіслано, чекай наступного turn-а".
- [x] Дитина не має завершувати turn звичайним `HERDR_RESULT` після питання — прописати це в `CHILD_PROTOCOL`.
- [x] Дописати `ask_question` до `--tools`, коли профіль задає `tools`. Механіка перевірена (див. «Гейт allowlist»): іншого шляху немає — дитина не може доввімкнути тулзу сама.
- [x] `ask_question` не блокує turn: пише файл і повертається. Жодних `ctx.ui.*` діалогів — інакше дитина лишиться `working` і батько не прокинеться до таймауту.
- [x] Батько перевіряє `question.md` **перед** читанням результату, у наявній гілці після `waitForAgent`. Нового очікування не додавати.
- [x] Формат результату батькові: label, профіль, текст питання, явна інструкція відповісти `herdr_agent` з тим самим `tabLabel`.
- [x] Не закривати one-shot target при `status: "question"`.
- [x] Чистити `question.md` перед кожним промптом (як `clearAgentResult`) — щоб старе питання не спрацювало двічі.
- [x] Reuse запаркованого one-shot: пейн переиспользується за label, коли на диску є невідповідане питання. Знайдено живим тестом, не тестами.
- [x] Тести: round-trip питання через файл, порожнє/пробільне питання, відсутність каналу, незалежність від result-файлу, прибирання разом із temp-текою, allowlist (4 випадки). Паралельні питання від двох агентів — не покрито.

### Верифікація

`bun test` — 95 pass / 0 fail (11 нових), `prettier --check` чисто, extension
завантажується (`pi -p`).

Живий цикл пройдено двічі, обидва рази без attach руками.

**Persistent:** агент із свідомо неоднозначним завданням викликав
`ask_question` → батько віддав питання раннім результатом, агент лишився
відкритим → відповідь тим самим `tabLabel` → агент завершив нормальним
`HERDR_RESULT`. Повторної віддачі питання не було, тобто `question.md`
коректно очищується перед промптом.

**One-shot:** те саме, плюс перевірка ідентичності пейна. Після питання —
1 керований агент `w2V:p3R` із `q=yes`. Після відповіді — 0 керованих
агентів, `panes: 1 splits: 0`. Якби відповідь пішла в новий пейн, запаркований
залишився б у списку; його там немає, отже reuse відпрацював і one-shot
закрився саме після справжнього завершення.

Перший прогін one-shot **провалився** саме тут: reuse був обмежений
`if (persistent)`, відповідь створила другий пейн, а перший осиротів. Юніт-тести
цього не бачили — кожна частина була коректна окремо, ламався стик. Це
аргумент за живий гейт у кожній фазі, а не лише за тести.

### Gate — пройдено

- [x] Повний цикл питання→відповідь→результат на живому агенті, без attach
      руками — і для `persistent`, і для `oneshot`.
- [x] Жоден шлях не лишає осиротілий pane після відповіді (перевірено
      порівнянням pane id до і після).
- [x] Запарковане питання не закриває one-shot передчасно.
- [x] `bun test` зелений, `prettier --check` чисто, extension завантажується.

### Ризики

- Дитина може спамити питаннями — обмежити одним відкритим питанням на агента.
- `--tools` allowlist: якщо Pi не пускає extension-тулзу через allowlist,
  фаза стопориться. Перевірити **першим кроком**, до всього іншого.

---

## Фаза 3 — Loadout snapshot

### Мета

Зафіксувати повний sandbox агента на момент спавну, щоб persistent-reuse і
майбутній resume не залежали від того, чи змінився профіль у
`~/.pi/agent/agents/*.md` між викликами.

### Дизайн

Зараз `state.ts` тримає lifecycle-стан (де pane, який label, який lifecycle),
але не **чим** агент був запущений. Якщо профіль відредагували, наступний
reuse промптить агента, який працює за старим системним промптом — а батько
думає, що за новим.

Фіксуємо в `HerdrAgentStateRecord`: `model`, `tools`, хеш системного промпту,
шлях до temp system-файлу, `cwd`.

### Файли

- `state.ts` — поля loadout у записі, версія схеми для міграції старих файлів;
- `index.ts` — писати loadout при спавні, порівнювати при reuse;
- `state.test.ts` — round-trip і читання запису без loadout (старий формат).

### Задачі

- [ ] Розширити `HerdrAgentStateRecord` полем `loadout`.
- [ ] Толерантне читання: записи без `loadout` лишаються валідними.
- [ ] При reuse порівняти поточний профіль із записаним; при розбіжності — попередити в результаті тулзи (не падати).
- [ ] Вирішити політику розбіжності: попередження чи перестворення агента. **Питання до користувача перед реалізацією.**

### Gate

`bun test` зелений, старий state-файл читається без помилок, розбіжність
профілю видно в результаті тулзи.

---

## Фаза 4 — Доставка результату асинхронного агента

### Що вже є, а чого немає

`wait: false` існує і працює: тулза шле промпт і одразу повертається
`… is still running` з `waited: false`. Тобто **паралельність є, доставки
немає**.

Перевірено живцем: агент, спавнений з `wait: false`, відпрацював, записав
318 символів у result-артефакт і став `idle`. Спостереження 28 с — до
оркестратора **не прийшло нічого**: ні tool-результату, ні повідомлення, ні
turn-а. Дізнатись про завершення вдалось лише зовнішньою shell-командою.
Забрати результат можна тільки самому: `herdr_agent` з тим самим `tabLabel`
і без `task` (re-wait) — він повернув повний `HERDR_RESULT`.

Отже збір ініціює батько. Якщо він забуде re-wait, робота дитини просто
пропадає, і ніщо про це не нагадає. Widget із фази 1 показує статус, але його
бачить **людина**, не модель — у контекст оркестратора він не потрапляє.

### Переоцінка обсягу

Початкова оцінка «фактично переписування `index.ts`» писалась **до** фази 1 і
більше не тримається. Фаза 1 вже дала те, чого бракувало: **фоновий
спостерігач**. Поллер widget-а тікає раз на секунду і вже знає статус кожного
керованого агента — а після того як тулза повернулась, більше ніхто за агентом
не дивиться.

Тому це вже не зміна архітектури, а надбудова над наявним поллером:

1. прапорець у state-записі «спавнений як `wait: false` і ще не зібраний»
   (напр. `detached: true`), який ставиться при ранньому поверненні і
   знімається після доставки. У state-файлі, а не в пам'яті — інакше не
   виживе `/reload`;
2. у тіку поллера: `detached` + статус `idle`/`done` + результат на диску →
   доставити, зняти прапорець;
3. доставка через `pi.sendMessage`, закриття one-shot після доставки.

Блокуючий шлях (`wait: true`) лишається як є. Це важливо: фаза 4 більше **не
скасовує** soft re-wait (`b6ee710`), дефолт `wait: true` і раннє повернення
питання з фази 2 — вона додає другий режим, а не замінює перший. Саме тут
початкова оцінка була найбільш хибною.

### Механіка доставки — перевірені факти

- `pi.sendUserMessage` **не підходить**: воно виглядає «як надруковане
  користувачем», що для нотифікації про завершення агента семантично
  неправильно, і воно завжди тригерить turn.
- `pi.sendMessage({ customType, content, display }, { triggerTurn: true,
  deliverAs: "steer" })` — правильний інструмент: кастомне повідомлення, яке
  бере участь у контексті LLM, з власним рендерером.
- `deliverAs: "steer"` (дефолт для `sendMessage`) доставляє **після того, як
  поточний assistant-turn закінчить свої tool-call-и, перед наступним викликом
  LLM**. Це знімає найгірший ризик: якщо оркестратор у цей момент блокується
  на іншому агенті, нотифікація стає в чергу, а не переплітається з очікуванням.
- Гард на стрімінг — `ctx.isIdle()`; `deliverAs` обов'язковий лише під час
  стрімінгу (`sendUserMessage` без нього кидає помилку). Патерн — у
  `examples/extensions/send-user-message.ts`.

### Задачі

- [x] `state.ts`: поле `detached?: boolean` у записі; ставиться при `wait:
      false`, знімається після доставки.
- [x] `index.ts`: `detached: !wait` виставляється в єдиному місці —
      `recordAgentLifecycle` на шляху спавну/reuse, а не в гілках повернення.
- [x] Поллер: детекція «detached + settled + є результат» → доставка. Крок
      доставки стоїть **перед** рендером, бо рендер має ранні виходи.
- [x] Доставка `sendMessage` з `customType: "herdr_agent_result"` +
      `registerMessageRenderer` (не `registerEntryRenderer` — той для
      `appendEntry`, який навмисно **не** бере участі в контексті LLM).
- [x] Закриття one-shot після доставки, і як наслідок — знято обмеження
      `oneshot ⇒ wait: true`. Лишилось лише для headless (`!ctx.hasUI`), де
      поллера немає, тож нікому ні доставити, ні закрити.
- [x] Дедуп: атомарний `claimDetachedAgent` — читання і знімання прапорця в
      одній locked read-modify-write. Claim беруться **до** відправки: провалена
      доставка втратить нотифікацію, але ніколи не продублює.
- [x] Синхронний збір (обидві гілки: wait і re-wait) теж звільняє claim.
      Інакше ручний re-wait і поллер доставили б результат двічі.
- [x] `GLOBAL_INSTRUCTIONS`: для `wait: false` результат прийде сам; плюс
      явна заборона починати делеговану роботу самому, поки detached-агент над
      нею працює.
- [ ] Питання (фаза 2) від detached-агента цим каналом не доставляється —
      лишається відкритим.

### Верифікація

`bun test` — 103 pass / 0 fail (8 нових: дедуп, гонка трьох одночасних claim,
персистентність прапорця через reload, settled-предикат), `prettier --check`
чисто, extension завантажується.

Живі прогони:

- **Доставка без re-wait.** One-shot з `wait: false` (раніше заборонена
  комбінація) відпрацював, результат прийшов **сам** новим turn-ом із
  префіксом `[herdr agent]`. Пейн закрився: `panes: 1 splits: 0`, запис зі
  стейту прибраний. Повторено на довгій задачі (7 файлів) — те саме.
- **Дедуп.** Спавн `wait: false`, одразу перехоплений ручним re-wait. Після
  збору `detached=false`, а результат **лишався на диску** — тобто саме та
  умова, за якої хибна реалізація доставила б удруге. Другої копії не
  надійшло.

### Gate — пройдено

- [x] Результат detached-агента доходить до оркестратора без жодної дії з його
      боку.
- [x] Detached one-shot закривається сам після доставки.
- [x] Рівно одна доставка при конкуренції поллера і ручного re-wait.
- [x] `bun test` зелений, `prettier` чисто, extension завантажується.

### Рішення

- **`triggerTurn` приймається.** Turn може піднятись, поки людина друкує —
  свідомо погоджено, як у форку. Ціна доставки нижча за ціну втраченого
  результату.

### Ризики

- **Гарантія «не дублюй делеговану роботу»** писалась для блокуючої моделі.
  При асинхронній оркестратор може почати ту саму роботу, поки дитина ще
  працює — інструкції треба уточнити.
- Дедуп через `/reload` — єдина справді нетривіальна частина.

### Що робити далі

Фаза 3 (loadout snapshot) більше не є передумовою для фази 4: доставка
спирається на поллер і state-прапорець, а не на снапшот loadout-а. Тому
порядок 3→4 можна змінити, і фаза 4 тепер має вищу цінність за фазу 3 —
вона закриває реальну діру (втрата результату), тоді як фаза 3 дає
попередження про розбіжність профілю.

---

## Спільні правила для всіх фаз

- Тести: `bun test` у теці extension-а; чисті функції тестуються, I/O — ні.
- Smoke: `bun build` (команда у фазі 1) — має пройти без помилок.
- **Новий файл у пакеті вимагає `stow --restow pi` перед reload.** Stow
  симлінкує пакет `pi` **per-file**, а не директорією, тому свіжий модуль
  існує в репо, але не в `~/.pi/agent/extensions/herdr-agents/`. Pi падає з
  `Failed to load extension: Cannot find module './<new>.ts'` і **зникають усі
  інструменти плагіна** — виглядає як «тулза пропала», а не як помилка
  компіляції. Кожна наступна фаза додає новий файл, тож послідовність завжди:
  `stow -d ~/dotfiles -t ~ --restow pi` → перевірка `pi -p "reply with OK only"`
  → `/reload`.
- Перевіряти завантаження extension-а треба headless: `pi -p "reply with OK only"`
  друкує помилку завантаження в stderr. `bun build` її не ловить, бо
  `@earendil-works/*` — зовнішні host-пакети (`pi-tui` лежить вкладеним у
  `pi-coding-agent/node_modules`, окремо не встановлений).
- Після зміни симлінкованих файлів: `/reload` або рестарт Pi.
- Оновлювати `AGENTS.md` і `docs/README.md` у тому ж коміті, що змінює поведінку.
- Формат коміта як у історії плагіна: `feat(herdr-agents): …`, `fix(herdr-agents): …`.
- Prettier зі стандартними налаштуваннями (`printWidth` 80). Комміт `a4b0d7c`
  у заголовку каже «72-char», але фактично файли чисті саме за дефолтом:
  `npx prettier@3 --check *.ts`.
