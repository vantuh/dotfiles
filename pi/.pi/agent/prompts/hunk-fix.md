---
description: Apply my Hunk review comments
---

Load the `hunk` skill and use `apply-comments` mode for the live Hunk session matching the current repository.

Defaults for this command:

- comment source: `user`;
- apply justified fixes in the current scope;
- do not remove human comments;
- run the relevant validation before reporting completion.

Treat additional command text as scope, constraints, or validation instructions:

$@
