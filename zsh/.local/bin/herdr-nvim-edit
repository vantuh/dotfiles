#!/usr/bin/env bash
# Thin shim: full logic lives in scripts/herdr-nvim-edit.ts.
set -euo pipefail

_self=$(realpath "$0" 2>/dev/null || echo "$0")
_dotfiles=$(cd "$(dirname "$_self")/../../.." && pwd)
_ts="$_dotfiles/scripts/herdr-nvim-edit.ts"

if command -v bun >/dev/null 2>&1; then
  exec bun "$_ts" "$@"
elif [[ -x "${HOME}/.bun/bin/bun" ]]; then
  exec "${HOME}/.bun/bin/bun" "$_ts" "$@"
else
  exec nvim "$@"
fi
