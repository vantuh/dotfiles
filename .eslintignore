# Only lint TypeScript sources (oxlint reads this automatically)
*
!*/
!**/*.ts
# node_modules re-included by !*/ — re-exclude after (last pattern wins)
**/node_modules/**
agents/
# herdr-managed state file — regenerated by herdr, don't lint/format
pi/.pi/agent/extensions/herdr-agent-state.ts
